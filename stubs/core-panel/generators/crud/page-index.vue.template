<script setup lang="ts">
import { Head, Link } from '@inertiajs/vue3'

import DummyModelClassController from '@/actions/App/Domains/DummyDomain/Http/Controllers/DummyModelClassController'
import AppLayout from '../../core-panel/Layouts/AppLayout.vue'

defineProps<{
    items: Array<{
        id: string
        name: unknown
        isActive: boolean
    }>
}>()
</script>

<template>
    <AppLayout>
        <Head title="DummyModelPluralLabel" />

        <div class="mx-auto grid max-w-7xl gap-6 px-4 py-8">
            <div class="flex items-center justify-between gap-4">
                <div class="grid gap-1">
                    <h1 class="text-2xl font-semibold text-[var(--cp-text-primary)]">DummyModelPluralLabel</h1>
                    <p class="text-sm text-[var(--cp-text-muted)]">Generated CRUD index page.</p>
                </div>

                <Link :href="DummyModelClassController.create.url()">
                    <Button label="Create DummyModelLabel" />
                </Link>
            </div>

            <div class="cp-card">
                <DataTable :value="items">
                    <Column field="name" header="Name" />
                    <Column field="isActive" header="Active" />
                </DataTable>
            </div>
        </div>
    </AppLayout>
</template>
