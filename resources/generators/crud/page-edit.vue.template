<script setup lang="ts">
import { Head, Form } from '@inertiajs/vue3'

import DummyModelClassController from '@/actions/App/Domains/DummyDomain/Http/Controllers/DummyModelClassController'
import AppLayout from '../../core-panel/Layouts/AppLayout.vue'

defineProps<{
    item: {
        id: string
        name: unknown
        isActive: boolean
    }
}>()
</script>

<template>
    <AppLayout>
        <Head title="Edit DummyModelLabel" />

        <div class="mx-auto grid max-w-4xl gap-6 px-4 py-8">
            <h1 class="text-2xl font-semibold text-[var(--cp-text-primary)]">Edit DummyModelLabel</h1>

            <Form :action="DummyModelClassController.update(item.id).url()" method="put" class="cp-card grid gap-4">
                <div class="grid gap-2">
                    <label class="text-sm font-medium text-[var(--cp-text-primary)]">Name</label>
                    <InputText name="name" :model-value="String(item.name ?? '')" />
                </div>

                <div class="flex items-center gap-2">
                    <Checkbox input-id="is_active" name="is_active" :binary="true" :model-value="item.isActive" />
                    <label for="is_active" class="text-sm text-[var(--cp-text-primary)]">Active</label>
                </div>

                <div class="flex justify-end">
                    <Button type="submit" label="Update" />
                </div>
            </Form>
        </div>
    </AppLayout>
</template>
