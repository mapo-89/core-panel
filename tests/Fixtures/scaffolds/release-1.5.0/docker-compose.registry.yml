services:
  app:
    image: ${PHP_IMAGE:?Set PHP_IMAGE}
    build: !reset null

  horizon:
    image: ${PHP_IMAGE:?Set PHP_IMAGE}
    build: !reset null

  scheduler:
    image: ${PHP_IMAGE:?Set PHP_IMAGE}
    build: !reset null

  nginx:
    image: ${NGINX_IMAGE:?Set NGINX_IMAGE}
    build: !reset null

  system-updater:
    image: ${UPDATER_IMAGE:?Set UPDATER_IMAGE}
    build: !reset null
